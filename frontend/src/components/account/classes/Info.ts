export module Info {
  export class Tutor {
    moto = new Default();
    about = new Default();
    conferenceLink = new Default();
    subjects = new Default();
    audience = new Default();
    languages = new Default();
  }

  class Default {
    errMsg = "";
  }
}
