// import helper from "@/store/modules/lesson/tutor/helper";

// jest.mock("@/main", () => ({
//   vm: { $l: () => {}, $router: { go: () => {} } },
// }));

// jest.mock("@/store/modules/lesson/tutor/module", () => ({
//   clearAll: () => {},
//   lessons: { offered: [] },
// }));

// jest.mock("@/services/lesson/tutor.service", () => {});
// jest.spyOn(window, "alert").mockImplementation(() => {});

// describe("helper.ts", () => {
//   it("test", () => {
//     expect(helper.exitIfFalsy(false, "")).toBeFalsy();
//   });
// });

